{
  "name": "react-static-starter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "react-static start",
    "stage": "react-static build --staging",
    "build": "react-static build",
    "serve": "serve dist -p 3000",
    "preinstall": "echo $NETLIFY_BUILD_BASE; if [[ -z \"$NETLIFY_BUILD_BASE\" ]]; then echo \"Not on Netlify, skipping.\"; else sed -i.bak 's/git@github\\.com/git:'$GITHUB_ACCESS_TOKEN'@github.com/' package.json && cat package.json; fi"
  },
  "dependencies": {
    "axios": "^0.16.2",
    "linc-front-end": "https://git@github.com/bitgenics/linc-front-end.git#master",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router": "^4.2.0",
    "react-static": "^5.9.7",
    "styled-components": "^3"
  },
  "devDependencies": {
    "babel-plugin-styled-components": "^1.3.0",
    "eslint-config-react-tools": "1.x.x",
    "serve": "^6.1.0"
  }
}
